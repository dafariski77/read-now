import { Colors } from "../constants/colors";
import { Spacing } from "../constants/spacing";
import { Roundness } from "../constants/roundness";
import { Typography, TypographyVariant, FontStyle } from "../constants/typography";

export { Colors, Spacing, Roundness, Typography, TypographyVariant, FontStyle };

export const Theme = {
  Colors,
  Spacing,
  Roundness,
  Typography,
};

export default Theme;
